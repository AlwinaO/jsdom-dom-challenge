let domChallenge = setInterval(() => {
//    if page has loaded, then increment counter
    document.addEventListener('DOMContentLoaded', (event) => {
        let counter = 0;
        while (counter < 100) {
            counter++
        }
    })
}, 1000);